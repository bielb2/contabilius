import { HomeNavigator } from 'src/navigation';

export const PrivateStackNavigator = (): JSX.Element => (
  <>
    <HomeNavigator />
    {/* <Route  component={RegisterScreen} /> */}
    {/* * <Route component={RecoverPasswordScreen} */}
  </>
);
